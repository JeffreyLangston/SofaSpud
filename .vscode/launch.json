{
  "configurations": [
    {
      "name": "Current TS File",
      "type": "node",
      "request": "launch",
      "program": "${workspaceRoot}/FundSyncService/node_modules/.bin/ts-node",
      "args": ["${relativeFile}"],
      "protocol": "inspector"
    }
  ]
}
